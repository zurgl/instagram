/* Layout */
#let layout(doc) = {
  set page(width: 800pt, height: 450pt, margin: (left: 80pt, y: 40pt), fill: black)
  set text(font: "Fira Code", weight: "semibold", size: 42pt, fill: white)
  set align(left + horizon)
  doc
}
